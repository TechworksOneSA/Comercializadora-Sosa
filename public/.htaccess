RewriteEngine On

# No reescribir assets
RewriteRule ^assets/ - [L]

# Si el archivo o carpeta existe, servirlo
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás a index.php
RewriteRule ^ index.php [QSA,L]
